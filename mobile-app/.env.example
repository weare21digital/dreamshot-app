# API Configuration
# For web development, use localhost or 127.0.0.1
# For Android emulator, use 10.0.2.2 (maps to host machine's localhost)
# For physical devices, use your machine's local network IP (e.g., 192.168.x.x)

# Web API URL (used when running on web browser)
EXPO_PUBLIC_API_URL_WEB=http://127.0.0.1:3000/api

# Android API URL (used on Android emulator or physical device)
# Use 10.0.2.2 for emulator, or your network IP for physical device
EXPO_PUBLIC_API_URL_ANDROID=http://192.168.10.124:3000/api

# iOS API URL (used on iOS simulator or physical device)
# Use localhost for simulator, or your network IP for physical device
EXPO_PUBLIC_API_URL_IOS=http://192.168.10.124:3000/api

# Physical device API URL (use your LAN IP — same as iOS URL for device testing)
EXPO_PUBLIC_API_URL_DEVICE=http://192.168.x.x:3000/api

# Google OAuth Configuration
# Create OAuth 2.0 Client IDs in Google Cloud Console (https://console.cloud.google.com/apis/credentials)
#
# 1. Create a "Web application" client - use this ID for EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID
#    Authorized JavaScript origins: http://localhost:8081, http://localhost:19006
#    Authorized redirect URIs: http://localhost:8081, http://localhost:19006
#
# 2. Create an "Android" client - use this ID for EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID
#    Package name: com.yourcompany.mobileapp (from app.json)
#    SHA-1 fingerprint: Get from your keystore or EAS
#
# 3. Create an "iOS" client - use this ID for EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID
#    Bundle ID: com.yourcompany.mobileapp (from app.json)

EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=your-web-client-id.apps.googleusercontent.com
EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=your-android-client-id.apps.googleusercontent.com
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=your-ios-client-id.apps.googleusercontent.com

# Note: Google Sign-In uses native SDK (@react-native-google-signin/google-signin),
# NOT expo-auth-session. No redirect URI needed. Requires native builds (not Expo Go).

# AI Photo Background Changer (optional feature)
# Provider selection (defaults to OpenAI): openai | fal
EXPO_PUBLIC_AI_IMAGE_PROVIDER=openai

# OpenAI provider config
# If EXPO_PUBLIC_AI_BG_API_URL is omitted, app defaults to OpenAI Images Edits endpoint.
# Provide either EXPO_PUBLIC_OPENAI_API_KEY or EXPO_PUBLIC_AI_BG_API_KEY.
EXPO_PUBLIC_AI_BG_API_URL=https://api.openai.com/v1/images/edits
EXPO_PUBLIC_OPENAI_API_KEY=your-openai-api-key
# EXPO_PUBLIC_AI_BG_API_KEY=your-openai-api-key
EXPO_PUBLIC_AI_BG_MODEL=gpt-image-1

# fal.ai provider config (scaffold only in this version)

# Optional demo mode: set to 1 to test UI/result/save/share flow without live API key.
# EXPO_PUBLIC_AI_BG_DEMO_MODE=1
